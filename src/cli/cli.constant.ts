export const MODULES_PATH = './cli/commands';

export const GET_VERSION_FILE_PATH = './package.json';

export const FILE_END_SUFFIXES = 'command.ts';

export const COMMAND_PREFIX = '--';

export const OPTION_PREFIX = '-';

export const ErrorMessage = {
  COMMAND_REGISTERED_MESSAGE: 'Command %commandName% is already registered.',
  COMMAND_NOT_REGISTERED_MESSAGE: 'The default command "%command%" is not registered.'
};
